<Window x:Class="CarRentals.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CarRentals"
        mc:Ignorable="d"
        Title="Вход" Height="400" Width="410">
    <Grid Background="#2C3E50">
        <Border Width="400" Height="350" 
                Background="White" 
                CornerRadius="10"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">

            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="КАРШЕРИНГ"  FontSize="20" 
                           FontWeight="Bold" Foreground="#2C3E50" TextAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Grid.Row="1" Text="Логин:" 
                           FontSize="14" Foreground="#34495E" Margin="0,0,0,5"/>

                <TextBox x:Name="txtLogin" Grid.Row="2" FontSize="14" Height="35" Margin="0,0,0,15" Padding="5"/>

                <TextBlock Grid.Row="3" Text="Пароль:" 
                           FontSize="14" Foreground="#34495E" Margin="0,0,0,5"/>

                <PasswordBox x:Name="txtPassword" Grid.Row="4" FontSize="14" Height="35" Margin="0,0,0,20" Padding="5"/>

                <Button x:Name="btnLogin" Grid.Row="5" 
                        Content="ВОЙТИ" FontSize="16" FontWeight="Bold" Height="40"
                        Background="#3498DB" Foreground="White"
                        Click="BtnLogin_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <TextBlock x:Name="txtError" Grid.Row="6" Text=""
                           Foreground="Red" TextAlignment="Center" Margin="0,10,0,0" FontSize="12"/>
            </Grid>
        </Border>
    </Grid>
</Window>
